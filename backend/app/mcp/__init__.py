"""M9: read-only MCP integration.

- :mod:`app.mcp.client` — MCP client manager (stdio / Streamable HTTP)
  with an explicit server allow-list and a read-only tool policy.
- :mod:`app.mcp.server` — the read-only MCP server exposing case-scoped
  social evidence, case summaries, artifacts and propagation graphs.
"""
